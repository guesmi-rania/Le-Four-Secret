{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "seed:admin": "node scripts/seedAdmin.js",
    "build:frontend": "npm --prefix ../frontend install && npm --prefix ../frontend run build && mkdir -p ./public/client && cp -r ../frontend/dist/* ./public/client/",
    "build:admin": "npm --prefix ../admiiin/admiiin install && npm --prefix ../admiiin/admiiin run build && mkdir -p ./public/admin && cp -r ../admiiin/admiiin/dist/* ./public/admin/",
    "build:all": "npm run build:frontend && npm run build:admin"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Backend Node.js pour Recettes de Cuisine",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.13.2",
    "nodemailer": "^7.0.6"
  }
}
